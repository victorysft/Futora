/* ── Private access portal background ── */
export default function CinematicBackground() {
  return (
    <div className="terminal-background">
      {/* Light sweep and grain are handled via CSS */}
    </div>
  );
}
